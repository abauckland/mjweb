<h1>Networking Information</h1>

<table class="table table-striped table-hover" style="width : 100%" >
  <thead>
    <tr>
      <th>Title</th>
      <th>Text</th>
      <th>Link text</th>
      <th>Link url</th>
      <th>Image</th>
      <th width="120px"></th>
    </tr>
  </thead>

  <tbody>
    <% @networkings.each do |networking| %>
      <tr>
        <td><%= networking.title %></td>
        <td><%= networking.text %></td>
        <td><%= networking.link_text %></td>
        <td><%= networking.link_url %></td>
        <td><%= image_tag(networking.image.image_url(:thumb)) if networking.image.image? %></td>
        <td>
            <div class="btn-group-xs">        
                <%= link_to 'Edit', edit_networking_path(networking), :class => "btn btn-sm btn-success"  %>
                <%= link_to 'Destroy', networking, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-sm btn-danger"  %>
            </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Item', new_networking_path, class: "btn btn-sm btn-success"  %>
<%= link_to "Back to Contents List", mjweb.contents_path, class: "btn btn-sm btn-warning btn pull-right" %>

