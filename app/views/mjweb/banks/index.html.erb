<h1>Credit Union Information</h1>

<table class="table table-striped table-hover" style="width : 100%" >
  <thead>
    <tr>
      <th>Title</th>
      <th>Text</th>
      <th>Link text</th>
      <th>Link url</th>
      <th>Image</th>
      <th width="120px"></th>
    </tr>
  </thead>

  <tbody>
    <% @banks.each do |bank| %>
      <tr>
        <td><%= bank.title %></td>
        <td><%= bank.text %></td>
        <td><%= bank.link_text %></td>
        <td><%= bank.link_url %></td>
        <td><%= image_tag(bank.image.image_url(:thumb)) if bank.image.image? %></td>
        <td>
            <div class="btn-group-xs">        
                <%= link_to 'Edit', edit_bank_path(bank), :class => "btn btn-sm btn-success"  %>
                <%= link_to 'Destroy', bank, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-sm btn-danger"  %>
            </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Item', new_bank_path, class: "btn btn-sm btn-success"  %>
<%= link_to "Back to Contents List", mjweb.contents_path, class: "btn btn-sm btn-warning btn pull-right" %>

