<style>
.image_delete{
  padding: 5px 0px 20px 0px;
}
</style>
  
<div class="row">

    <div class="col-lg-12">
        <h3 class="page-header">Loaded Font Images</h3>
    </div>
 
    <% if @fonts %>        
        <% @fonts.each do |font| %>               
            <div class="col-lg-3 col-md-4 col-sm-4 col-xs-6 thumb">  
                <%= image_tag(font.font, {:width => "100px", :height => "100px"}) if font.font? %>
                <div class="image_delete"><%= link_to 'Destroy', font, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-xs btn-danger"  %></div>
            </div>
        <% end %>    
    <% else %>
        <div class="website_images">
            No font images have been uploaded.
        </div>
    <% end %>
</div>
<%= link_to 'Upload Font', new_font_path, class: "btn btn-sm btn-success"  %>
            