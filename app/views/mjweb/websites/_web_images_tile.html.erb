
<div class="tile_container">
    <div class="images_tile">
        <% if images %>
            <% images.each do |image| %>
                <%= image_tag(image.image, :class => "rotating-item") %>       
            <% end %>
        <% end %>
    </div>
</div>                       