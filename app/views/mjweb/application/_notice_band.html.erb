<% unless flash.empty? %>
    <div class="row">
        <% if flash.key?("notice") %>
            <div class="notice_band">
                <p>Notice: <%= flash.notice %></p>
            </div>
        <% end %>
    </div>
<% end %>